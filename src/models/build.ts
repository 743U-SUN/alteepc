// このファイルは型定義を@/types/buildにリダイレクトします
import { PCBuild, CompatibilityIssue, SavedBuild, BuildShareInfo } from '@/types/build';

// 後方互換性のために型をエクスポート
export type { PCBuild, CompatibilityIssue, SavedBuild, BuildShareInfo };
